
<div class="hero-unit">
            <h1>Lets Meet up</h1>
            <p>This is a template for a simple marketing or informational website. It includes a large callout called the hero unit and three supporting pieces of content. Use it as a starting point to create something more unique.</p>
			<% if !session[:username].blank? %>
			<p><%= link_to("Create an event",{:action => 'new', :userId => params[:id], :name => params[:First_name], :eventId => ""}, :class => 'btn btn-primary btn-large') %></p>
			<% else %>
			<p><%= link_to("Sign me up!",{:action => 'new', :controller => 'Users'}, :class => 'btn btn-primary btn-large') %></p>
			<% end %>
</div>
<div class="row-fluid">
<table class="table table-hover">
<tr>
<th>Event Name</th>
<th>Event Location</th>
<th>Event Date</th>
<th>Event Created on</th>
<th>More details</th>
</tr>
<% @event.each do |event| %>
<tr>
<td><%= event.name %></td>
<td><%= event.Location %></td>
<td><%= event.Date %></td>
<td><%= event.created_at %></td>
<td><%= link_to("Show more details", {:action => 'show', :id => event.id})%></td>
</tr>
<% end %>
</table>
          
			
</div>

